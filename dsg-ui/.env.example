VITE_DEFAULT_ACCOUNT_ID=demo-acct
VITE_API_BASE=/dsg/v1

# RingCentral OAuth credentials live on the backend (application-local.yml).
# Redirect URI must match dsg.rc.redirect-uri in backend config.
# See docs/api/rc-oauth-dev-setup.md
